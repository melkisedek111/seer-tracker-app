import { CustomRequest } from "../..";
import { allowedOrigins } from "../../config/allowedOrigins";

export const credentials = (request: any) => {
    // const headers: any = request.headers;
    // if (headers.host) {
    //     if(allowedOrigins.includes(headers.host)) {
    //         request.set.headers["Access-Control-Allow-Credentials"] = true
    //     }
    // }
}